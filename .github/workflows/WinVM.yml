name: Simple Windows Run

on: [push]

jobs:
  just-run-it:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Banana
        # This runs it on a real Windows VM. No Wine, no Docker, no x11 nonsense.
        run: .\build\Banana.exe
