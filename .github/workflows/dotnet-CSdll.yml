name: Build and Decompile DLL

on:
  push:
    branches:
      - "**"

permissions:
  contents: write

jobs:
  build-dotnet:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      # -------------------------------
      # CHECKOUT
      # -------------------------------
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # -------------------------------
      # CONFIG VALIDATION
      # -------------------------------
      - name: Validate dotnet.config.js
        run: |
          if [ ! -f "./dotnet.config.js" ]; then
            echo "❌ dotnet.config.js not found in repo root"
            echo "pwd = $(pwd)"
            ls -a
            exit 1
          fi

          node - << 'EOF'
          const cfg = require("./dotnet.config.js");

          const required = ["commitDll", "commitCs", "CI", "Advanced"];
          for (const key of required) {
            if (!(key in cfg)) {
              console.error(`❌ Missing required config field: ${key}`);
              process.exit(1);
            }
          }

          const adv = cfg.Advanced;
          const advRequired = ["target", "output", "commit2"];
          for (const key of advRequired) {
            if (!(key in adv)) {
              console.error(`❌ Missing Advanced.${key}`);
              process.exit(1);
            }
          }

          if (!cfg.CI) {
            console.error("❌ CI disabled by config");
            process.exit(1);
          }

          if (!adv.target.endsWith(".cs")) {
            console.error("❌ Advanced.target must be a .cs file");
            process.exit(1);
          }

          if (!adv.output.endsWith(".dll")) {
            console.error("❌ Advanced.output must be a .dll file");
            process.exit(1);
          }

          console.log("✔ dotnet.config.js validated");
          EOF

      # -------------------------------
      # SOURCE VALIDATION
      # -------------------------------
      - name: Validate target source file
        run: |
          TARGET=$(node -e "console.log(require('./dotnet.config.js').Advanced.target)")

          if [ ! -f "./$TARGET" ]; then
            echo "❌ Target source file not found: $TARGET"
            exit 1
          fi

          if [ ! -s "./$TARGET" ]; then
            echo "❌ Target source file is empty: $TARGET"
            exit 1
          fi

      # -------------------------------
      # BUILD
      # -------------------------------
      - name: Install .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Build DLL
        run: |
          TARGET=$(node -e "console.log(require('./dotnet.config.js').Advanced.target)")
          OUTPUT=$(node -e "console.log(require('./dotnet.config.js').Advanced.output)")

          rm -rf temp build
          mkdir build temp

          cp "./$TARGET" build/Program.cs
          cp build/Program.cs temp/Program.cs

          dotnet new console -n temp --force --no-restore
          rm temp/Program.cs
          mv build/Program.cs temp/

          dotnet build temp -c Release

          DLL_PATH=$(find temp/bin/Release -name "temp.dll" | head -n 1)
          cp "$DLL_PATH" "./$OUTPUT"

          echo "✔ Built DLL → $OUTPUT"

      # -------------------------------
      # DECOMPILE
      # -------------------------------
      - name: Install ILSpy
        run: dotnet tool install --global ilspycmd

      - name: Decompile DLL
        run: |
          OUTPUT=$(node -e "console.log(require('./dotnet.config.js').Advanced.output)")
          ilspycmd "./$OUTPUT" -o "./dll.source.cs"
          echo "✔ Decompiled → dll.source.cs"

      # -------------------------------
      # CONDITIONAL COMMIT
      # -------------------------------
      - name: Conditional commit
        run: |
          BRANCH=$(git rev-parse --abbrev-ref HEAD)
          TARGET_BRANCH=$(node -e "console.log(require('./dotnet.config.js').Advanced.commit2)")

          if [ "$BRANCH" != "$TARGET_BRANCH" ]; then
            echo "⏭ Skipping commit: branch mismatch ($BRANCH != $TARGET_BRANCH)"
            exit 0
          fi

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          OUTPUT=$(node -e "console.log(require('./dotnet.config.js').Advanced.output)")

          # Add DLL if enabled
          if node -e "process.exit(require('./dotnet.config.js').commitDll ? 0 : 1)"; then
            git add "./$OUTPUT"
          fi

          # Add decompiled source if enabled
          if node -e "process.exit(require('./dotnet.config.js').commitCs ? 0 : 1)"; then
            git add "./dll.source.cs"
          fi

          git commit -m "CI: update outputs from latest build" || echo "No changes to commit"
          git push
